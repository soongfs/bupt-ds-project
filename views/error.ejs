<!DOCTYPE html>
<html>
<head>
    <title>错误</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f8f8f8;
            color: #333;
        }
        h1 {
            color: #e74c3c;
        }
        pre {
            background-color: #f2f2f2;
            padding: 10px;
            border-radius: 5px;
            overflow-x: auto;
            white-space: pre-wrap;
            word-wrap: break-word;
        }
    </style>
</head>
<body>
    <h1>发生错误</h1>
    <p><%= message %></p>

    <%
    // 判断是否显示堆栈信息（假设通过环境变量 NODE_ENV 控制）
    const showStack = process.env.NODE_ENV !== 'production';
    if (showStack && error) {
    %>
        <h2>错误详情</h2>
        <pre><%= error.stack %></pre>
    <% } %>
</body>
</html>